<div class='row'>
  <div class='col-md-6'>
    <%= image_tag @book.image.url(:original), class:'img-responsive' %>
  </div>
  <div class='col-md-6'>
    <h3>Event Name: <%= @book.name %></h3>
    <h3>Location: <%= @book.author %></h3>
    </br><h4>Description: </h4><p><%= @book.description %></p>
    </br><h4>Comments: </h4><p><%= @book.description %></p>
  </div>
</div>
<!--    <% if current_user %>
    <%= form_tag buy_path(slug: @book.slug) do %>
        <form action="" method="POST">
          <script
            src="https://checkout.stripe.com/checkout.js" class="stripe-button"
            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
            data-amount="<%= @book.price %>"
            data-name="eStuk"
            data-description="<%= @book.name %>"
            data-email="<%= current_user.email%>">
          </script>
        </form>
    <% end %>
    <% else %>
    <p>Please Login First to Buy </p>
    <% end %>
-->
<!--<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @book.name %>
</p>

<p>
  <strong>Author:</strong>
  <%= @book.author %>
</p>

<p>
  <strong>Description:</strong>
  <%= @book.description %>
</p>

<p>
  <strong>Price:</strong>
  <%= @book.price %>
</p>

<p>
  <strong>Availability:</strong>
  <%= @book.availability %>
</p>
-->

